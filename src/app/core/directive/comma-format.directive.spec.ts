import { CommaFormatDirective } from './comma-format.directive';

describe('CommaFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaFormatDirective();
    expect(directive).toBeTruthy();
  });
});
